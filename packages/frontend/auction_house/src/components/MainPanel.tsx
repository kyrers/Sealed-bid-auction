import React, { MouseEventHandler } from 'react';
import { Button } from "react-bootstrap";

type FunctionProps = {};

function MainPanel({}: FunctionProps) {

    return (
        <div className="main-panel">
            Main Panel
        </div>
    );
}

export default MainPanel;